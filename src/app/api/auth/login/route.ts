import { NextResponse } from "next/server";
export async function POST(request: Request) { const body = await request.json(); if (String(body.username).toLowerCase() !== "amira" || String(body.password) !== "rwaq2026") return NextResponse.json({ error: "Incorrect username or password" }, { status: 401 }); const response = NextResponse.json({ user: { name: "Amira Mansour", initials: "AM", role: "Admin" } }); response.cookies.set("rwaq_session", "rwaq-demo-session", { httpOnly: true, sameSite: "lax", secure: process.env.NODE_ENV === "production", maxAge: 60 * 60 * 8, path: "/" }); return response; }
